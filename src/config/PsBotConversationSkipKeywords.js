const skipConversationKeywords = [
    "args received",
    "Intent Received",
    "Entities Received",
    "Entities Filled up with",
];

export default () => {
    return skipConversationKeywords;
};